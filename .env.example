# Copy to .env and fill in. The real .env is gitignored and never committed.

# Supabase (project settings -> API)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Cloudflare R2 — basemap tiles + avatar delivery
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_TILES=chingo-tiles
R2_BUCKET_AVATARS=chingo-avatars

# fal.ai — pixel avatar generation. Server-side only, never shipped to the client.
FAL_KEY=
