{
  "name": "chingo-presence",
  "private": true,
  "type": "module",
  "description": "Live presence for ChinGo: one Durable Object per 600 m region, positions in memory and nowhere else.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5",
    "typescript": "^5",
    "vitest": "^3",
    "wrangler": "^4"
  }
}
