# Copy to .dev.vars for `wrangler dev`. The real file is gitignored.
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_SERVICE_ROLE_KEY=
# Only for a project that still signs access tokens with HS256. Leave empty otherwise.
SUPABASE_JWT_SECRET=
